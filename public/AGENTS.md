# AGENTS.md — public

Manifest and public assets rules:
- Manifest must be MV3 compliant.
- Permissions must be least-privilege.
- CSP must stay strict (avoid broad allowances).
- Any new host_permissions must be justified.
