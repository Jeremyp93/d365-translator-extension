/**
 * Re-export usePendingChanges hook from context for convenience.
 * This allows importing from hooks directory: import { usePendingChanges } from '@hooks/usePendingChanges'
 */
export { usePendingChanges, PendingChangesProvider } from '../context/PendingChangesContext';
